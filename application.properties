# Unified application properties
# Shared Micronaut settings
# micronaut.application.name is set via system properties in build.gradle

# Shared logging settings
client.rootpath=.
client.resourcespath=resources
client.logpath=logs
client.dataininame=DATA.INI
client.usepathmappings=true
client.autoextract=true
client.enablesearch=true
client.cache.max-files=5000
client.cache.max-memory-mb=1024
client.cache.warmup.enabled=true
client.cache.warmup.limit=500
client.public-url=${CLIENT_PUBLIC_URL:http://localhost:3338}

# Proxy settings (used by both client and wsproxy)
wsproxy.enabled=${ENABLE_WSPROXY:true}
wsproxy.allowed-targets=${WS_ALLOWED_TARGETS:127.0.0.1:6900,127.0.0.1:6121,127.0.0.1:5121}
micronaut.server.port=8081

# Startup tasks
client.tasks.validate-grfs=${VALIDATE_GRFS:false}
client.tasks.generate-mapping=${GENERATE_MAPPING:false}
client.tasks.mapping-output=${MAPPING_OUTPUT:path-mapping.json}
