package turoran.grfloader.loader;

/**
 * Options for GRFNode
 */
public class GRFNodeOptions extends GrfOptions {
    /** Use buffer pool for better performance (default: true) */
    public boolean useBufferPool = true;
}
